<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <Description>Thin typed HTTP client for EggIdentity.Host, shared by EggIncognito and EggLedger so neither hand-rolls its own wrapper.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\EggIdentity.Contract\EggIdentity.Contract.csproj" />
  </ItemGroup>

</Project>
