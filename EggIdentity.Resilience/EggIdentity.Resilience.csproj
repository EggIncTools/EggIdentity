<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <Description>Dependency-free resilience primitives for eggidentity: exponential backoff with full jitter, retry, deadlines and a small circuit breaker. Everything takes a TimeProvider so tests never sleep.</Description>
  </PropertyGroup>

</Project>
